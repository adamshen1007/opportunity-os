## Summary

-

## Linked Issue

-

## Type of Change

- [ ] Repository foundation
- [ ] Documentation
- [ ] Architecture/specification
- [ ] Implementation
- [ ] Test-only

## Verification

- [ ] `node scripts/verify-repository.mjs --phase review`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] `pnpm test`

## Security And Hygiene

- [ ] I checked `git status --short --ignored`.
- [ ] I did not commit `.env`, secrets, credentials, tokens, private keys, database dumps, or generated local artifacts.
- [ ] `.env.example` contains placeholders only.
- [ ] Logs, screenshots, archives, and generated reports contain no secrets or private data.
- [ ] If event contracts changed, I completed event privacy review: no raw payloads, secrets, tokens, API keys, provider keys, credentials, DSNs, auth headers, or production transports were introduced.
- [ ] If database files changed, I completed database migration and secret-safety review: migrations are scoped and reviewed, no prohibited tables or production event store transport were introduced, and no `DATABASE_URL`, credentials, SQL payloads, auth headers, provider details, or Prisma internals are exposed.
- [ ] If domain files changed, I completed domain contract review: public exports route through `packages/domain/src/index.ts`, dependencies stay within approved shared infrastructure packages, no concrete business aggregates/events/payloads/scoring/workflows/app services/database repositories were introduced, and future packages are not directed to bypass `@opportunity-os/domain`.
- [ ] If application files changed, I completed application contract review: public exports route through `packages/application/src/index.ts`, dependencies stay within approved foundation packages, no REST APIs/controllers/auth/connectors/AI workflows/database repositories/frontend/scoring/product use cases were introduced, and future packages are not directed to bypass `@opportunity-os/application`.
- [ ] If container files changed, I completed DI and composition review: public exports route through `packages/container/src/index.ts`, dependencies stay within approved foundation packages, lifetimes remain `singleton`/`scoped`/`transient`, config binding receives explicit typed config, logger binding does not introduce a singleton or app integration, no runtime container/service locator/reflection/app startup was introduced, and future packages are not directed to bypass `@opportunity-os/container`.
- [ ] If infrastructure files changed, I completed infrastructure composition review: module registrations, dependency graph contracts, lifecycle contracts, startup/shutdown contracts, health aggregation contracts, and foundation package composition metadata remain declarative; no REST APIs/controllers/auth/connectors/AI workflows/database repositories/frontend/product workflows/application services/business logic were introduced; and future packages are not directed to bypass `@opportunity-os/infrastructure`.
- [ ] If connector SDK files changed, I completed connector SDK boundary review: public exports route through `packages/connectors/src/index.ts`, dependencies stay within approved foundation packages, metadata/config/lifecycle/registry/factory/validation contracts remain generic, connector errors remain secret-safe, no Reddit/YouTube/OAuth/HTTP clients/APIs/auth/AI workflows/frontend/business logic/concrete connectors/connector execution were introduced, and future packages are not directed to bypass `@opportunity-os/connectors`.
- [ ] If connector runtime files changed, I completed connector runtime boundary review: public exports route through `packages/connector-runtime/src/index.ts`, dependencies stay within approved foundation packages, pipeline/state/policy/telemetry/metrics/aggregation/error/test harness contracts remain generic, telemetry and runtime outputs remain secret-safe, no Reddit/YouTube/OAuth/HTTP clients/scheduler/queue/worker process/APIs/auth/AI workflows/frontend/business logic/provider integration/actual connector execution were introduced, and future packages are not directed to bypass `@opportunity-os/connector-runtime`.
- [ ] If connector host files changed, I completed connector host boundary review: public exports route through `packages/connector-host/src/index.ts`, dependencies stay within approved foundation packages, bootstrap/orchestration/lifecycle/binding/startup/shutdown/health/execution/result/error/test harness contracts remain generic, host outputs remain secret-safe, no Reddit/YouTube/OAuth/HTTP clients/scheduler/queue/worker process/APIs/auth/AI workflows/frontend/business logic/provider integration/actual connector execution were introduced, and future packages are not directed to bypass `@opportunity-os/connector-host`.
- [ ] If Reddit connector files changed, I completed Reddit connector boundary review: public exports route through `packages/connectors-reddit/src/index.ts`, dependencies stay within approved connector foundation packages, metadata/capability/config/validation/data/factory/host/error/fixture contracts remain contract-only, OAuth and credential fields remain sensitive contract fields only, no live Reddit calls, HTTP clients, scraping, scheduler, queue, worker process, database persistence, AI workflows, APIs, frontend, business logic, provider integration, or actual connector execution were introduced, and future Reddit connector implementation is not directed to bypass `@opportunity-os/connectors-reddit`.
- [ ] If Reddit runtime files changed, I completed Reddit runtime review: public exports route through `packages/connectors-reddit/src/index.ts`, dependencies stay within approved connector foundation packages, runtime construction uses fake provider/fixtures only, config validation is explicit and does not read `process.env`, lifecycle/read/result/error/harness behavior remains deterministic and secret-safe, no OAuth/live Reddit calls/HTTP clients/scraping/scheduler/queue/worker/database persistence/AI workflows/APIs/frontend/business logic/provider integration/event publishing/host startup/runner loop/external connector execution were introduced, and future Reddit provider work is not directed to bypass `@opportunity-os/connectors-reddit`.
- [ ] If Reddit provider transport files changed, I completed Reddit provider transport boundary review: provider exports route through `packages/connectors-reddit/src/provider/index.ts` and the package root, existing runtime exports remain stable, implementation stays inside `packages/connectors-reddit`, OAuth secrets and sensitive fields remain redacted, request/response descriptions are safe, raw provider responses are not leaked or persisted, fake transport tests cover the changed behavior, and no Raw Content persistence, AI workflows, opportunity generation, REST APIs, frontend, scheduler, worker, database persistence, or business logic was introduced.
- [ ] If raw-content files changed, I completed Raw Content Pipeline Foundation review: public exports route through `packages/raw-content/src/index.ts`, dependencies stay within approved foundation packages, source/provenance metadata uses safe references, storage ports remain interface-only, raw-content events remain contract-only, Reddit mapping does not execute provider calls, fixtures are deterministic and secret-free, errors remain stack-safe and token-safe, security/dependency-boundary/stability tests cover the changed surface, and no persistence implementation, Prisma repository, normalization algorithm, hashing engine, event bus, AI workflow, opportunity generation, REST API, frontend, scheduler, worker, database persistence, or business scoring was introduced.
- [ ] If normalization files changed, I completed Normalization Pipeline Foundation review: public exports route through `packages/normalization/src/index.ts`, dependencies stay within approved foundation packages, canonical text/cleaning/language/chunking/preservation/validation/result/event contracts remain declarative, fixtures are deterministic and secret-free, security/dependency-boundary/export-stability/pipeline tests cover the changed surface, and no embeddings, LLMs, AI analysis, semantic interpretation, DOM/browser behavior, network calls, parser-library integrations, event bus, persistence implementation, Prisma repository, opportunity generation, REST API, frontend, scheduler, worker, database persistence, or business scoring was introduced.
- [ ] If embedding files changed, I completed Embedding Foundation review: public exports route through `packages/embeddings/src/index.ts`, dependencies stay within approved foundation packages, embedding/provider/request/response/chunk/metadata/validation/cache/result/error/event contracts remain provider-independent, fixtures use deterministic synthetic vectors only, security/dependency-boundary/export-stability/contract-stability/pipeline tests cover the changed surface, and no OpenAI, Gemini, Voyage, provider SDK, provider API call, vector database, model execution, AI reasoning, prompt execution, cache persistence, opportunity generation, REST API, frontend, scheduler, worker, or business logic was introduced.
- [ ] If LLM Analysis files changed, I completed LLM Analysis Foundation review: public exports route through `packages/llm-analysis/src/index.ts`, dependencies stay within approved foundation packages, provider/prompt/template/input/output/structured-output/analysis/validation/safety/redaction/result/error/event contracts remain provider-independent, fixtures use deterministic synthetic prompts, normalized content references, embedding references, and structured outputs only, security/dependency-boundary/export-stability/contract-stability/pipeline tests cover the changed surface, and no OpenAI, Anthropic, Gemini, provider SDK, provider API call, live LLM call, prompt runtime, extraction workflow, pain point extraction, opportunity generation, REST API, frontend, persistence, scheduler, worker, business scoring, real prompt, real embedding, provider payload, API key, or network behavior was introduced.
- [ ] If Structured Analysis files changed, I completed Structured Analysis Foundation review: public exports route through `packages/analysis/src/index.ts`, dependencies stay within approved foundation packages, structured analysis/parser/schema/normalization/evidence/confidence/provenance/validation/result/error/event contracts remain provider-independent, fixtures use deterministic synthetic inputs, outputs, evidence, confidence metadata, provenance, and validation examples only, security/dependency-boundary/export-stability/contract-stability/pipeline tests cover the changed surface, and no provider SDK, live provider call, prompt execution, AI reasoning, pain point extraction, opportunity generation, REST API, frontend, persistence, scheduler, worker, business scoring, provider payload, API key, real network behavior, or business example was introduced.
- [ ] If Opportunity Engine files changed, I completed Opportunity Engine Foundation review: public exports route through `packages/opportunity-engine/src/index.ts`, dependencies stay within approved foundation packages, opportunity primitive/source/evidence/hypothesis/score/confidence/ranking/validation/result/error/event contracts remain provider-independent, fixtures use deterministic synthetic IDs, source/evidence references, hypotheses, score metadata, confidence metadata, ranking metadata, validation examples, results, and events only, security/dependency-boundary/export-stability/contract-stability/upstream-integration tests cover the changed surface, and no REST API, frontend, persistence, scheduler, worker, live AI call, prompt runtime, billing, user account, production ranking algorithm, scoring implementation, extraction workflow, opportunity generation logic, business workflow, provider payload, prompt, secret, or business example was introduced.
- [ ] If Opportunity Pipeline files changed, I completed Opportunity Pipeline Foundation review: public exports route through `packages/opportunity-pipeline/src/index.ts`, dependencies stay within approved upstream foundation packages, pipeline primitives/stages/metadata/provenance/aggregation/assembly/candidate/validation/result/error/event/fixture contracts remain provider-independent, fixtures are deterministic and synthetic, security/dependency-boundary/export-stability/contract-stability/upstream-integration tests cover the changed surface, and no business scoring algorithm, ranking algorithm, recommendation engine, REST API, frontend, persistence, scheduler, worker, provider SDK, workflow engine, aggregation algorithm, generation logic, execution behavior, provider payload, prompt, secret, or production business example was introduced.
- [ ] If Candidate Opportunity files changed, I completed Candidate Opportunity Engine Foundation review: public exports route through `packages/opportunity-candidates/src/index.ts`, dependencies stay within approved upstream foundation packages, candidate primitive/lifecycle/metadata/provenance/evidence completeness/confidence aggregation/validation/result/error/event/fixture contracts remain provider-independent, fixtures are deterministic and synthetic, security/dependency-boundary/export-stability/contract-stability/upstream-integration tests cover the changed surface, and no production ranking algorithm, recommendation engine, business scoring, REST API, frontend, persistence, scheduler, worker, provider SDK, execution behavior, provider payload, prompt, secret, or production business example was introduced.
- [ ] If Opportunity Generation files changed, I completed Opportunity Generation Workflow Foundation review: public exports route through `packages/opportunity-generation/src/index.ts`, dependencies stay within approved upstream foundation packages, generation primitive/input/output/service/assembly/validation/confidence/result/error/event/fixture contracts remain provider-independent, fixtures are deterministic and synthetic, security/dependency-boundary/export-stability/contract-stability/upstream-integration/service tests cover the changed surface, and no production ranking algorithm, recommendation engine, business scoring, REST API, frontend, persistence, scheduler, worker, billing, user account, provider SDK, live AI provider, prompt execution, provider payload, prompt, secret, or production business example was introduced.
- [ ] If Opportunity Ranking files changed, I completed Opportunity Ranking Engine review: public exports route through `packages/opportunity-ranking/src/index.ts`, dependencies stay within approved upstream foundation and opportunity packages, ranking decisions remain deterministic/testable/explainable, score calculation is reconstructable from explicit signals/factors/weights, tie breaking is stable and documented, fixtures are deterministic and synthetic, security/dependency-boundary/export-stability/contract-stability/ranking-quality/upstream-integration tests cover the changed surface, and no recommendation engine, REST API, frontend, persistence, scheduler, worker, billing, user account, provider SDK, ML behavior, LLM call, hidden heuristic, prompt, provider payload, secret, or production business example was introduced.
- [ ] If REST API files changed, I completed REST API review: public exports route through `apps/api/src/index.ts`, dependencies stay within approved upstream Opportunity OS packages, route definitions/OpenAPI/request-response envelopes/validation/pagination/filtering/error mapping remain deterministic, authentication and authorization remain contract-based unless a later milestone scopes a production provider, opportunity and ranking routes use explicit ports, fixtures are synthetic, security/contract-stability/dependency-boundary/integration tests cover the changed surface, and no frontend, billing, user management, analytics, notifications, production authentication provider, persistence change, scheduler, worker, provider SDK, secret, provider payload, or unrelated product workflow was introduced.
- [ ] If Dashboard MVP files changed, I completed dashboard review: implementation stays inside `apps/web`, the app remains a strict TypeScript Next.js App Router app, dashboard routes/navigation/list/detail/ranking/evidence/search/filter/pagination/state views remain deterministic, UI code consumes the web API integration layer rather than bypassing API routes, fixtures are synthetic, security/dependency-boundary/route-stability/component/browser tests cover the changed surface, Playwright coverage remains runnable without production services, rendered output does not expose secrets, raw provider payloads, prompts, stacks, or unsafe internals, and no authentication implementation, billing, analytics, notifications, user account, production deployment, persistence change, recommendation engine, mobile app, scheduler, worker, provider SDK, or unrelated backend change was introduced.
- [ ] If Product Validation Loop files changed, I completed validation-loop review: implementation remains deterministic product validation only, feedback vocabulary/routes/UI consume `apps/api` and `apps/web` boundaries, feedback values are safe for design-partner walkthroughs, fixtures are synthetic, and no production persistence, billing, analytics platform, notification, email integration, CRM integration, scheduler, worker, mobile app, complex admin console, secret, raw provider payload, prompt, stack, or unsafe internal detail was introduced.
- [ ] If design-partner validation files changed, I confirmed the walkthrough in `docs/04_IMPLEMENTATION/04-003_DESIGN_PARTNER_WALKTHROUGH.md` is current, demo states are deterministic, feedback can be collected without production services, and participant/private notes are not committed.
- [ ] If Private Beta files changed, I completed Private Beta review: deployment readiness uses `phase-3-milestone-29`, deployment architecture is documented, `.github/workflows/deploy.yml` remains a readiness gate until a later scoped task attaches a hosting provider and protected secrets, production config is placeholder-only, config binding is documented, secrets management/health monitoring/operational logging/monitoring strategy/backup strategy are documented and secret-safe, rollback guidance is documented, the operational runbook and beta checklist are current, invite-only authentication stores invite hashes rather than raw invite codes, session management is scoped to Private Beta access, beta feedback and bug reports store only safe validation fields, protected dashboard/onboarding/save-dismiss/bug-reporting/invite workflow behavior is deterministic, `apps/api` and `apps/web` remain the API and dashboard boundaries, production secrets are not committed or logged, and no billing, multi-tenancy, production identity provider, enterprise auth, payments, subscriptions, enterprise features, notifications, CRM integration, email, business logic changes, or unrelated product system was introduced.
- [ ] If Phase 3 Milestone 30 Beta Operations files changed, I completed Beta Operations review: `phase-3-milestone-30` passes, the change is operations-only, README/CONTRIBUTING/documentation index/roadmap/implementation order/scripts documentation stay consistent, operator and beta-user docs remain current, operational workflows/checklists/troubleshooting are safe, support notes avoid secrets and participant private data, and no new backend features, AI features, APIs, dashboard features, persistence features, authentication features, payments, CRM integrations, notifications, analytics platforms, mobile apps, schedulers, workers, or unscoped product behavior was introduced.
- [ ] I cleaned up unrelated local files before opening this PR.

## Checklist

- [ ] I read the relevant Engineering Kit documents.
- [ ] I kept cross references valid.
- [ ] I updated documentation where needed.
- [ ] I did not introduce business logic unless this PR is explicitly scoped for implementation.
- [ ] I did not introduce connectors, APIs, or AI workflows unless approved by specification.
