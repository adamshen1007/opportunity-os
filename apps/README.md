# Apps

Future application entry points live here.

No application code is present in the repository foundation.

## Future Testing

No app tests exist during Phase 0.

When application work begins:

- API app tests should use Supertest for HTTP routes and request/response contracts.
- Web app tests should use Playwright for end-to-end user workflows.
- App-level integration tests should cover interactions with shared packages, PostgreSQL, Redis, and external-service adapters only when those dependencies exist.

Do not add application tests before the corresponding app implementation task is approved.
