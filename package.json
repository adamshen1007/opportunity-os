{
  "name": "opportunity-os",
  "version": "0.0.0",
  "private": true,
  "description": "Documentation-first engineering foundation for Opportunity OS.",
  "license": "UNLICENSED",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=24 <25",
    "pnpm": "11.7.0"
  },
  "scripts": {
    "repo:check": "node scripts/verify-repository.mjs --phase phase-1-milestone-2",
    "lint": "node scripts/verify-repository.mjs --phase phase-1-milestone-2 && turbo run lint",
    "build": "node scripts/verify-repository.mjs --phase phase-1-milestone-2 && turbo run build",
    "test": "node scripts/verify-repository.mjs --phase phase-1-milestone-2 && turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  }
}
