{
  "name": "opportunity-os",
  "version": "0.0.0",
  "private": true,
  "description": "Documentation-first engineering foundation for Opportunity OS.",
  "license": "UNLICENSED",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=24 <25",
    "pnpm": "11.7.0"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "dev:api": "pnpm --filter @opportunity-os/api dev",
    "dev:web": "pnpm --filter @opportunity-os/web dev",
    "repo:check": "node scripts/verify-repository.mjs --phase review",
    "lint": "node scripts/verify-repository.mjs --phase review && turbo run lint",
    "build": "node scripts/verify-repository.mjs --phase review && turbo run build",
    "test": "node scripts/verify-repository.mjs --phase review && turbo run test --concurrency=1 -- --testTimeout=90000"
    ,"smoke:external": "node scripts/verify-external-mvp.mjs"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  }
}
